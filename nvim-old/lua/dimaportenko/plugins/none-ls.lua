return {
  "nvimtools/none-ls.nvim", -- for formatters and linters
   -- config = function()
   --    local null_ls = require("null-ls")
   --
   --    null_ls.setup({
   --        sources = {
   --            null_ls.builtins.table,
   --        },
   --    })
   -- end
    -- opts = function(_, opts)
    --   local nls = require "null-ls"
    --   table.insert(opts.sources, nls.builtins.formatting.style)
    -- end,
}
